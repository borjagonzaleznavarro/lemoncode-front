interface SwitchRoutes {
  root: string;
  cats: string;
  dogs: string;
  checkout: string;
}

export const switchRoutes: SwitchRoutes = {
  root: '/',
  cats: '/cats',
  dogs: '/dogs',
  checkout: '/checkout',
};